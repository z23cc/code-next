# Repository Policy

- This repository exists to implement the `aiwf` workflow kernel.
- Treat `.ai/` as the workflow source of truth.
- Keep the Python kernel small, auditable, and file-protocol driven.
- Prefer minimal, well-tested changes over speculative abstractions.
- Record blockers and assumptions explicitly so later milestones can resume safely.
